
## Классы

```python
# Строки в Python — это объекты. Мы вызываем метод строки, функцию, связаную со строкой.

upper_name2 = 'ABC'.upper() # можно вызвать метод непосредственно для объекта

name = 'ABC'
upper_name1 = name.upper() # можно использовать переменную
```